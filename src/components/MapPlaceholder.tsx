import "./MapPlaceholder.css";

export default function MapPlaceholder() {
  return (
    <div className="map-placeholder">
      <span className="map-placeholder__label">MAP AREA — precip / reflectivity tiles</span>
    </div>
  );
}
